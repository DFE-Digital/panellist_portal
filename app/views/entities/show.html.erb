<p>
  @odata.context <%= @entity["@odata.context"] %>
</p>

<% @entity["value"].each do |hash| %>
  <table>
    <% hash.each do |k,v| %>
      <tr>
        <td><%= k %></td>
        <td><%= v %></td>
      </tr>
    <% end %>
  </table>

  ===
  <br>
<% end %>
